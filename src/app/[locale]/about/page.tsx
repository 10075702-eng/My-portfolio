import { media } from '@/config/media'; import { t, type Locale } from '@/config/i18n';
export default async function About({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; const l = locale as Locale; return <main className="page-pad min-h-screen pt-32 pb-24"><p className="mono text-[10px] text-acid">ABOUT JUNHANG ZHOU</p><div className="mt-6 grid gap-10 md:grid-cols-2"><h1 className="text-5xl md:text-7xl">{l === 'zh' ? '以镜头为起点，漫游真实与虚拟。' : 'English artist statement to be provided.'}</h1><img src={media.avatar} alt="Junhang Zhou" className="aspect-[4/5] w-full object-cover grayscale"/></div></main> }
