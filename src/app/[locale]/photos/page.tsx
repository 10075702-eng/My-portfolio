import { PhotosLibrary } from '@/components/photos-library'; import { t, type Locale } from '@/config/i18n';
export default async function PhotosPage({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; const l = locale as Locale; return <main className="page-pad min-h-screen pt-32 pb-24"><p className="mono text-[10px] tracking-[.2em] text-acid">PHOTOGRAPHS & STILLS</p><h1 className="mt-4 text-5xl md:text-7xl">{t(l).nav.photos}</h1><p className="mt-6 max-w-xl text-sm leading-7 text-white/60">{l === 'zh' ? '从地区开始，观看静帧与照片记录。' : 'Browse visual records by region, grouped into stills and photographs.'}</p><div className="mt-12"><PhotosLibrary locale={l}/></div></main> }
