import { PostcardsWall } from '@/components/postcards-wall'; import { t, type Locale } from '@/config/i18n';
export default async function Postcards({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; const l = locale as Locale; return <main className="page-pad min-h-screen pt-32 pb-24"><p className="mono text-[10px] tracking-[.2em] text-acid">POSTCARD WALL</p><h1 className="mt-4 text-5xl md:text-7xl">{t(l).nav.postcards}</h1><p className="mt-5 max-w-xl text-sm text-white/55">{l === 'zh' ? '翻看正反面，为喜欢的画面打分，或标记“想要”。' : 'Browse both sides, rate the postcards you like, or mark one as wanted.'}</p><div className="mt-16"><PostcardsWall locale={l}/></div></main> }
