import { WorkCard } from '@/components/work-card'; import { earlyWorks, formalWorks } from '@/config/works'; import { t, type Locale } from '@/config/i18n';
export default async function Work({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; const l = locale as Locale; const copy = t(l); return <main className="page-pad min-h-screen pt-32 pb-24"><p className="mono text-[10px] tracking-[.2em] text-acid">SELECTED WORK</p><h1 className="mt-4 text-5xl md:text-7xl">{copy.nav.work}</h1><div className="mt-16 grid gap-x-6 gap-y-14 sm:grid-cols-2">{formalWorks.map((work, index) => <WorkCard key={work.slug} locale={l} index={index} work={{ slug: work.slug, title: work[l].title, category: work[l].category, year: work.year, cover: work.media.cover, previewVideo: work.media.previewVideo, video: work.media.video }}/>)}</div><section className="mt-28 border-t border-white/15 pt-7"><h2 className="text-3xl">{copy.earlyWork}</h2><div className="mt-9 grid grid-cols-2 gap-4 md:grid-cols-4">{earlyWorks.map(work => <article key={work.title.zh}><img src={work.cover} alt="" className="aspect-[4/3] w-full object-cover"/><h3 className="mt-3 text-sm">{work.title[l]}</h3><p className="mt-1 text-xs text-white/45">{work.category[l]}</p></article>)}</div></section></main> }
