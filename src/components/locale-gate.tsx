'use client';
import { useEffect, useState } from 'react'; import { useRouter } from 'next/navigation';
export function LocaleGate() { const router = useRouter(); const [ready, setReady] = useState(false); useEffect(() => { const saved = localStorage.getItem('zjx-locale'); if (saved === 'zh' || saved === 'en') router.replace(`/${saved}`); else setReady(true); }, [router]); const select = (locale: 'zh' | 'en') => { localStorage.setItem('zjx-locale', locale); router.replace(`/${locale}`); }; if (!ready) return <main className="min-h-screen bg-ink"/>; return <main className="page-pad grid min-h-screen place-items-center bg-ink text-center"><div><p className="mono text-[10px] tracking-[.25em] text-acid">JUNHANG ZHOU / VISUAL CREATOR</p><h1 className="mt-5 text-5xl tracking-[-.09em] md:text-7xl">周俊行 <span className="font-display italic text-acid">/ Junhang Zhou</span></h1><p className="mt-5 text-sm text-white/55">请选择浏览语言 / Choose your language</p><div className="mx-auto mt-12 grid w-full max-w-xl gap-3 sm:grid-cols-2"><button onClick={() => select('zh')} className="border border-white/35 px-8 py-8 text-2xl transition hover:border-acid hover:bg-acid hover:text-black">中文</button><button onClick={() => select('en')} className="border border-white/35 px-8 py-8 text-2xl transition hover:border-acid hover:bg-acid hover:text-black">English</button></div></div></main> }
