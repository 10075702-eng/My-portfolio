'use client';
import { useState } from 'react'; import { media } from '@/config/media'; import type { Locale } from '@/config/i18n';
type Short = typeof media.shorts[number];
export function ShortsGallery({ locale }: { locale: Locale }) { const [selected, setSelected] = useState<Short | null>(null); const title = (item: Short) => item.title[locale] || item.title.zh; return <><div className="shorts-masonry">{media.shorts.map(item => <button key={item.id} onClick={() => setSelected(item)} className="group mb-5 block w-full break-inside-avoid text-left"><div className={`relative overflow-hidden bg-zinc-900 ${item.aspect === 'portrait' ? 'aspect-[9/16]' : 'aspect-video'}`}><img src={item.cover} alt="" className="h-full w-full object-cover"/><span className="mono absolute bottom-3 right-3 rounded-full bg-black/60 px-2 py-1 text-[10px]">{item.duration}</span></div><h2 className="mt-3 text-sm">{title(item)}</h2></button>)}</div>{selected && <div className="fixed inset-0 z-[60] grid place-items-center bg-black/90 p-5" onClick={() => setSelected(null)}><div className={`w-full ${selected.aspect === 'portrait' ? 'max-w-[430px]' : 'max-w-[1100px]'}`} onClick={event => event.stopPropagation()}><button onClick={() => setSelected(null)} className="mb-3 text-xs">{locale === 'zh' ? '关闭 ×' : 'Close ×'}</button><video autoPlay controls playsInline className={`w-full bg-black ${selected.aspect === 'portrait' ? 'aspect-[9/16]' : 'aspect-video'}`}><source src={selected.video}/></video><p className="mt-3 text-sm">{title(selected)}　{selected.duration}</p></div></div>}</> }
