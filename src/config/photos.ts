import type { Locale } from './i18n';
export type PhotoItem = { id: string; category: 'still' | 'photo'; region: string; title: { zh: string; en: string }; note: { zh: string; en: string }; src: string; thumb: string };
export const photoRegions: Record<string, Record<Locale, string>> = { '重庆': { zh: '重庆', en: 'Chongqing' }, '新疆': { zh: '新疆', en: 'Xinjiang' }, '大连': { zh: '大连', en: 'Dalian' }, '香港': { zh: '香港', en: 'Hong Kong' }, '上海': { zh: '上海', en: 'Shanghai' }, '九寨沟': { zh: '九寨沟', en: 'Jiuzhaigou' } };
const seed = [{ region: '重庆', pinyin: 'chongqing' }, { region: '新疆', pinyin: 'xinjiang' }, { region: '大连', pinyin: 'dalian' }, { region: '香港', pinyin: 'xianggang' }, { region: '上海', pinyin: 'shanghai' }, { region: '九寨沟', pinyin: 'jiuzhaigou' }];
/** 每个地区 2 张静帧、3 张照片；替换或新增条目无需修改组件。 */
export const photos: PhotoItem[] = seed.flatMap(({ region, pinyin }) => [1, 2, 3, 4, 5].map(index => ({ id: `${pinyin}-${String(index).padStart(2, '0')}`, category: index <= 2 ? 'still' as const : 'photo' as const, region, title: { zh: `${region} / 占位${index <= 2 ? '静帧' : '照片'} ${String(index).padStart(2, '0')}`, en: `${photoRegions[region].en} / Placeholder ${index <= 2 ? 'Still' : 'Photograph'} ${String(index).padStart(2, '0')}` }, note: { zh: '', en: '' }, src: `/media/photos/${pinyin}-${String(index).padStart(2, '0')}.jpg`, thumb: `/media/photos/${pinyin}-${String(index).padStart(2, '0')}.jpg` })));
