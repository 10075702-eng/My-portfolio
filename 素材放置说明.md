# 素材放置说明

## 核心规则（只有两步）

```
第一步：把文件放进 public/media/ 对应的文件夹
第二步：把网址改成 /media/ 开头的路径
```

**路径写法**：从 `media` 开始写，前面加斜杠。例如文件放在
`public/media/postcards/dalian-01-front.jpg`，配置里就写
`/media/postcards/dalian-01-front.jpg`。

**不要写** `C:\...` 或 `public/media/...`。

---

## 一、对照表：什么内容放哪里

| 内容 | 文件夹 | 命名规则 | 需要改的配置 |
|---|---|---|---|
| **明信片正反面** | `postcards/` | `dalian-01-front.jpg` / `-back.jpg` | `media.ts`（路径**已写好**，放文件即可） |
| 首页背景视频 | `hero/` | 随便 | `media.ts` → `heroVideo` |
| 作品封面图 | `covers/` | `echoes.jpg`、`marshal.jpg` 等 | `media.ts` → `workCovers` |
| 作品悬停预览视频 | `covers/` | `echoes-preview.mp4` | `media.ts` → `previewVideos` |
| 短视频 / Vlog | `shorts/` | 随便（建议英文） | `media.ts` → `shorts` |
| 360 全景照片 | `panoramas/` | `pano-01.jpg` | `media.ts` → `panoramas` |
| 360 全景视频 | `panoramas/` | `pano-01.mp4` | `media.ts` → `panoramas` |
| Echoes 章节配图 | `works/echoes/` | 已经是 `page-01~20.jpg` | `works.ts` → sections |
| 挂帅 配图 | `works/marshal/` | 随便 | `works.ts` → sections |
| The Missing Original 配图 | `works/missing/` | 随便 | `works.ts` → sections |
| 全景作品配图 | `works/panorama/` | 随便 | `works.ts` → sections |

---

## 二、明信片命名对照（72 张）

```
dalian-01-front.jpg  ~  dalian-08-front.jpg      大连
liangshan-01-front.jpg ~ liangshan-08-front.jpg  凉山
huobajie-01-front.jpg ~ huobajie-08-front.jpg    火把节
jiuzhaigou-01-front.jpg ~ jiuzhaigou-08-front.jpg 九寨沟
shanghai-01-front.jpg ~ shanghai-08-front.jpg    上海
weihai-01-front.jpg  ~  weihai-08-front.jpg      威海
xianggang-01-front.jpg ~ xianggang-08-front.jpg  香港
xianggelila-01-front.jpg ~ xianggelila-08-front.jpg 香格里拉
shengri-01-front.jpg ~ shengri-08-front.jpg      生日
```

背面同理，把 `front` 换成 `back`。

**每张明信片需要两个文件**：正面 + 背面。
如果你的明信片只有正面，告诉我，我让 Codex 改成可选的。

---

## 三、尺寸规格（重要）

| 类型 | 规格 | 说明 |
|---|---|---|
| **360 视频** | **最大 4096×2048**，宽高比严格 2:1 | 超过的话手机上无法显示 |
| **360 照片** | 8192×4096 可用 | 没有 4096 限制 |
| 首页 Hero 视频 | 1920×1080，8–15 秒，3–5MB | 单独压缩，静音循环 |
| 普通图片 | 宽度 1800px 以内，JPEG 质量 85 | 单张控制在 300KB 内 |
| 预览短视频 | 1080p，5–10 秒，1–3MB | 悬停时播放用 |

---

## 四、视频的特别说明

**大视频文件不要放进这个文件夹然后提交到 GitHub。**
Git 不适合存大文件，几十 MB 的视频会让仓库变得很臃肿。

现在的做法：
- **开发阶段**：视频先放本地 `public/media/`，方便调试
- **上线之前**：视频改放到外部托管（Cloudflare R2 之类），
  配置里填完整网址

所以你在 `media.ts` 里可能会看到两种写法，都是对的：
- `/media/shorts/xxx.mp4` ← 本地文件
- `https://你的存储域名/xxx.mp4` ← 外部托管

---

## 五、图片压缩方法

如果图片太大，可以用这个命令批量压缩（需要装 Python + Pillow）：

```bash
python -c "
from PIL import Image
import os
d = 'public/media/postcards'
for f in os.listdir(d):
    if f.lower().endswith(('.png','.jpg','.jpeg')):
        p = os.path.join(d, f)
        im = Image.open(p).convert('RGB')
        if im.width > 1800:
            im = im.resize((1800, round(im.height*1800/im.width)), Image.LANCZOS)
        im.save(p, 'JPEG', quality=85, optimize=True)
print('完成')
"
```

或者直接把图片发我，我帮你压。
