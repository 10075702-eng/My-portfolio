# 作品集网站 — 进度与交接单

> 更新于：明天继续之前的收尾。给用户和 Codex 看。

## 线上地址

https://my-portfolio-mu-three-41.vercel.app

更新流程：改配置 → `git push` → Vercel 自动部署（约 1 分钟）。
推送前先 `git pull --rebase`（用户有时会在 GitHub 网页上直接改 README）。

---

## 已完成

| 板块 | 状态 |
|---|---|
| 正式作品 4 个 | Echoes / 挂帅 / The Missing Original / **舱门背后的光**（含封面、文字、章节配图、完整影片播放）|
| 首页播放按钮 | 4 个正式作品卡片居中播放按钮，点击开弹层看完整影片 |
| 短视频 34 个 | 1080p 完整时长（Vlog 未截断）、**按时间倒序**、分类+年份两排筛选 |
| 明信片 56 张 | 9 系列，打分 + 想要 |
| 全景展厅（无人机）| **8 张**：新疆 3 / 九寨沟 2 / 重庆 2 / 郑州 1 |
| 关于 / 联系 | 已写 |

---

## 进行中（明天收尾）

### 照片与静帧

- 素材已处理完毕：163 张，3.83GB → **55.5MB**
- **已全部上传 R2 并验证**（326 个文件：`photos/full/` + `photos/thumb/`，均返回 HTTP 206 image/jpeg）
- 分类结果：**照片 7 张 / 静帧 156 张**
- 地区：大连 60 / 香港 103
- 地点：air3s 31、pocket3 25、insta 4（大连）；九龙 52、大澳 30、M+美术馆 16、大佛 5（香港）
- 中间产物：`%TEMP%\photo_out\manifest.json`（含每张的地区、地点、类型、宽高）

**待办**：把 manifest 写入 `src/config/photos.ts`，然后部署。

### 已定方案

**地区 → 地点小节 → 瀑布流**，每张卡片标「静帧 / 照片」小标签。

已发给 Codex 的指令要点：PhotoItem 增加 `place` 字段；地点小节标题；
标签中英对照（静帧 = Still，照片 = Photograph）；弹层沿用现有的。

---

## 关键技术事实（排错时看）

**R2 托管**（免费 10GB，不限期）
- 桶：`portfolio-videos`
- 公开入口写在 `media.ts` 的常量 `R2`（**换域名只改这一处**）
- 凭据：`C:\Users\ZJH\r2-credentials.txt`（Account ID / Key / Secret / 公开网址）

**为什么全景图片走代理**
全景查看器用 WebGL 加载纹理，**强制要求 CORS**；R2 公开域名不返回 CORS 头。
所以 `next.config.js` 里有一条 rewrite：`/r2/*` → R2。
浏览器看到的是同源请求，问题消失。**视频不需要**（`<video>` 不受 CORS 限制）。

**资源路径规则**
```
/r2/films/*.mp4      作品完整影片
/r2/previews/*.mp4   悬停预览（10 秒静音）
/r2/*.mp4            短视频（34 个）
/r2/panoramas/*.jpg  360 原片
/r2/thumbs/*.jpg     全景缩略图
/r2/photos/full/*.jpg    照片与静帧 — 显示图
/r2/photos/thumb/*.jpg   照片与静帧 — 缩略图
```

**本机限制**
- `npm run dev` 与 `npm run build` **不能同时跑**（共用 `.next` 会损坏）
- 验证一律走生产模式：`npm run build` → `npm start`
- 含 U+2019 撇号的文件名无法在 Bash 直接传参，用 Python `glob`
- 网络对 `supabase.co` 与 `r2.dev` 偶有不稳定，需重试

---

## 待办清单

| 优先级 | 事项 |
|---|---|
| 🔴 | 照片与静帧写入配置 + 部署（素材已就绪）|
| 🟠 | 全景相机拍的全景（用户还没导）|
| 🟡 | 《舱门背后的光》的工具列表（UE5？Blender？）|
| 🟡 | 「城市之窗」的封面（现在还是错的，挂着川剧图）|
| 🟡 | 茶韵乡兴封面底部有烧入字幕，可换一帧干净的 |
| 🟢 | 社交账号（Instagram / 小红书）|
| 🟢 | 大连明信片上那条 ★5.0 测试评分要清掉吗 |
